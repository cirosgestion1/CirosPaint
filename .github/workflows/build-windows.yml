name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [build/verify-0.10.10]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/build-windows-01010.yml
